import React from 'react';

const Footer = ({ children }) => {
  return (
    <footer>
      <div>
        <b>Placeholder for footer</b>
        {children}
      </div>
    </footer>
  );
};

export default Footer;
